[project]
name = "gradpath-mcp"
version = "0.1.0"
description = "GradPath AI MCP adapter"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.28.1",
]

[project.scripts]
gradpath-mcp = "gradpath_mcp:main"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"
